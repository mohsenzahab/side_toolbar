name: side_toolbar
description: A playable and customizable and easy-to-use toolbar made with flutter
version: 0.0.3
homepage: https://github.com/mohsenzahab/side_toolbar

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
